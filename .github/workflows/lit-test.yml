name: LIT Test

on:   
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  check-sdir:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install ninja-build

    - name: Load MLIR cache
      id: mlir-cache
      uses: actions/cache@v2
      with:
        path: llvm-project
        key: ${{ runner.os }}-mlir-commit

    - name: Check MLIR cache
      if: steps.mlir-cache.outputs.cache-hit != 'true'
      uses: spcl/mlir-dace/.github/workflows/install_mlir.yml

    - name: Build & Test
      run: |
        mkdir build
        cd build
        cmake -G Ninja .. -DMLIR_DIR=${{github.workspace}}/llvm-project/build/lib/cmake/mlir -DLLVM_EXTERNAL_LIT=${{github.workspace}}/llvm-project/build/bin/llvm-lit
        cmake --build . --target check-sdir

      
